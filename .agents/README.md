# Agents

This directory contains agent configurations and definitions.

## Structure

- `agents/` - Individual agent configuration files
- `shared/` - Shared utilities and configurations

## Usage

Add agent definitions as JSON or YAML files in the `agents/` subdirectory.
